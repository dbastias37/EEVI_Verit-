{
  "name": "eevi-verite",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "flask run",
    "build:chat-widget": "npm --prefix static/chat-widget ci && npm --prefix static/chat-widget run build"
  }
}
